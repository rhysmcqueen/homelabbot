services:
  homelabbot:
    image: ghcr.io/rhysmcqueen/homelabbot:latest
    container_name: homelabbot
    restart: unless-stopped
    network_mode: host
    env_file: .env
    volumes:
      - ./data:/app/data
